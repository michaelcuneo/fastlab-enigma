import styled from 'styled-components';

import { animated as a } from 'react-spring';

export const LinkDetails = styled(a.div)`
  font-family: 'archiaregular', sans-serif;
  font-size: 18px;
  line-height: 22px;
  color: white;
`;
