import styled from 'styled-components';

import { animated as a } from 'react-spring';

export const LinkArrow = styled(a.div)`
  display: flex;
  float: right;
  position: relative;
  padding: 0px 20px 0px 0px;
  color: white;
`;
